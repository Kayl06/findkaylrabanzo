# Formspree: use the form ID (the hash from the URL, e.g. mvzboblp from https://formspree.io/f/mvzboblp).
# Use NEXT_PUBLIC_ so the form submits from the browser — Formspree works best this way.
# NEXT_PUBLIC_FORMSPREE_FORM_ID=mvzboblp
